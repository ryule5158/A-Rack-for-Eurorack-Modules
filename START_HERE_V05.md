# 从这里开始：Rack4Modules V0.5 零基础说明

这是一套面向 3 排 104HP Eurorack 模块的原创便携机箱。当前设计版本是
**V0.5 被夹持双剪切折叠脚架版**：V0.4 的模块区、接口、提手、VESA 和电源预留保持
不变，左右外置并先轴向外弹的脚架则由内外侧板夹持的固定平面折腿取代。

V0.5 的功能组织参考了同类产品把折腿藏进侧板的做法，但双剪切轴系、夹层宽度和
60°/75°几何都是 Rack4Modules 自己的工程选择，不是对 Intellijel 内部结构的精确复制。

## 1. 应该打开哪个文件

先进入：

`C:\Users\LENOVO\Desktop\Rack4Modules\cad\assemblies`

正常查看和继续设计时，优先打开：

`Rack4Modules_OpenCase_V05.SLDASM`

如果双击没有打开，请先启动 SOLIDWORKS 2025，再选择“文件 → 打开”。不要新建空白工程，
也不要把总装单独移出项目目录。`.SLDASM` 依赖 `cad/parts` 内的 `.SLDPRT`；交给加工厂
或另一台电脑时，应复制整个 `Rack4Modules` 文件夹。

当前 V0.5 目标总装如下：

| 文件名 | 用途 |
| --- | --- |
| `Rack4Modules_OpenCase_V05.SLDASM` | 正常开箱状态，也是主要设计入口 |
| `Rack4Modules_TransportClosed_V05.SLDASM` | 检查深盖、盖锁和运输包络 |
| `Rack4Modules_ClearanceCheck_V05.SLDASM` | 检查模块深度及内部预留避让 |
| `Rack4Modules_DesktopTilt60_V05.SLDASM` | 观察模块面相对桌面约 60° 的静态姿态 |
| `Rack4Modules_DesktopTilt75_V05.SLDASM` | 观察模块面相对桌面约 75° 的静态姿态 |

这些目标文件已经生成，并由 `reports/layout-v05-validation.md` 完成软件级复核：
`524 PASS / 15 WARNING / 0 FAIL`。15 项 WARNING 是样机、供应商硬件、公差链及连续运动等
仍需实体确认的边界，不等同于 CAD 失败。两份倾角总装中的桌面只是参照面，不属于产品、
BOM 或重量。

在 SOLIDWORKS 中可按住鼠标中键旋转、滚轮缩放，按 `F` 使模型适合窗口。第一次检查
应以观察为主；修改前先另存新版本并通过 Git 保留变更。

## 2. V0.5 解决了什么问题

V0.4 折腿位于侧框外侧，转动前需要沿主轴方向外移约 7 mm；主轴附近的长收纳槽又让
侧框难以形成完整的内侧承力耳板。V0.5 取消这条运动路线，改成每侧一套被夹持的结构盒：

```text
模块空间 | 3 mm 内侧承力框 | 4.8 mm 腔（4 mm 腿） | 3 mm 外承力颊板 | 外侧
```

腿在收纳、转动、60°和75°状态始终位于固定平面，不需要先向外弹。主轴同时受到内外两
块结构板支承，避免只有单侧薄板悬臂夹持。外颊板是受力件，不是装饰盖；每侧四个间隔柱
把它与内框连成盒状结构。

## 3. 必须认清的冻结尺寸

| 项目 | V0.5 名义值 |
| --- | ---: |
| Eurorack 行数 | `3 × 3U` |
| 每行宽度 | `104HP` |
| 1U 行 | `0` |
| 模块安装内净宽 | `542 mm` |
| V0.5 名义裸机总宽 | `563.6 mm` |
| 内侧承力框 | `3 mm 6061-T6` |
| 折腿 | `4 mm 6061-T6` |
| 腿腔 | `4.8 mm`，腿两侧名义间隙各 `0.4 mm` |
| 外承力颊板 | `3 mm 6061-T6` |
| 折腿中心平面 | `x=±276.4 mm` |
| 最外表面 | `x=±281.8 mm` |
| 主轴 | 名义 `Ø8 mm`，双剪切支承 |
| 间隔柱 | 每侧 4 个、全机 8 个，名义长度 `4.8 mm` |
| 主轴位置 | `y=-129 mm, z=52 mm` |
| 主轴到脚端有效长度 | `150 mm` |

主轴完整光杆必须跨越内框和外颊板两个剪切面，螺纹不得落在剪切面中。量产还要选定
法兰轴套、垫片和轴向防脱，不能让不锈钢轴直接长期磨损阳极氧化铝孔，也不能把主轴直接
攻入 3 mm 铝板。

`563.6 mm` 是当前名义裸机宽度，不是合盖运输包络。深盖、阳极层、板厚公差、平面度、
密封条和装配预载的最坏情况尺寸必须重新核对后才能出加工图。

## 4. 两个倾角如何理解

| 模块面相对桌面 | 腿从收纳位旋转 | 后支撑距离 |
| ---: | ---: | ---: |
| `60°` | 约 `101.375°` | 约 `102.830 mm` |
| `75°` | 约 `113.439°` | 约 `82.432 mm` |

这两组数据只定义静态 CAD 姿态。它们不证明折腿能连续无干涉地展开，也不证明满载时的
重心、防滑、止挡、锁销和桌面接触已经安全。特别是 75° 档，必须装入最重目标模块并
实测支撑多边形；设计目标是在测得的支撑边界内保留至少 20 mm 重心投影余量。

## 5. 锁止机构还不能直接加工

当前索引销零件只是概念包络，不能据此购买或加工量产锁销。尚未冻结的内容包括：

- 索引销供应商、真实销径、弹簧、手柄、轴向保持和锁孔耐磨衬套；
- 收纳、60°、75°三个位置的真实锁孔/凸轮/拨动几何及完全啮合反馈；
- 两个展开档的宽面实体硬止挡、接触面积和止挡载荷路径；
- 误触解锁、半啮合、夹手和两侧操作顺序。

索引销只能保持档位，不能单独承受整机工作载荷。60°和75°必须另有实体硬止挡，使主载荷
通过腿根和宽面止挡的压紧接触传递。

## 6. 出图或找加工厂前必须完成

1. 生成并复核 `reports/layout-v05-validation.md`，确认五套原生总装、STEP、引用、零部件
   计数、542 mm 净宽、563.6 mm 名义宽度及离散姿态没有未解释的 FAIL。
2. 冻结真实轴、轴套、间隔柱、紧固件、三档索引机构、硬止挡、防滑脚垫和防松方法。
3. 做从收纳到60°、再到75°的连续扫掠，检查侧板、盖锁、桌面、线缆和夹手区。
4. 完成板厚、阳极层、轴套配合、平面度、污染物和紧固预载的最坏公差链。
5. 对满载样机进行偏载、侧向冲击、永久变形、孔椭圆化、疲劳、跌落、防松、防滑、
   抗倾覆和最坏重心测试。

当前曾采用 20 kg 满载、冲击系数 2、单腿约 300 N，并附加 400 N 和 30 N 侧向工况
进行早期筛查。这只能说明 4 mm 腿相对 3 mm 草案更稳妥，不能称为有限元认证、结构认证
或实体测试通过。

## 7. 同类产品参考的证据边界

[Intellijel 7U Performance Case Gen-2 官方产品页](https://intellijel.com/shop/cases/7u/7u-performance-case-gen-2/)
及[官方手册](https://intellijel.com/downloads/manuals/7u-performance-case-gen-2_manual_2026.04.13.pdf)
能够确认其两条腿藏在侧板内，并使用独立弹簧 Stand Lock，提供两个使用位置。公开资料没有
公布剖面、双剪切结构、轴径、材料、额定载荷或疲劳寿命。

所以本项目可以说“参考了内藏脚架的功能组织”，不能说“精确复制 Intellijel”、
“Intellijel 也采用 Ø8 双剪轴”或“拥有与其相同的承载能力”。

## 8. 文档与版本识别

当前 V0.5 设计依据：

- [项目总览](README.md)
- [V0.5 被夹持双剪切脚架说明](docs/layout-v05-captured-stand.md)
- [V0.5 机器可读增量参数](design/parameters-v0.5-captured-stand.json)
- `reports/layout-v05-validation.md`：最终软件验证入口，统计为 `524 PASS / 15 WARNING / 0 FAIL`

[V0.4 零基础说明](START_HERE_V04.md)、[V0.4 布局说明](docs/layout-v04-original.md)及
`*_V04` CAD 文件是历史基线。V0.4 验证报告的 `340 PASS / 4 WARNING / 0 FAIL`
不能作为 V0.5 的验证结果。V0.3 及更早版本同样只用于追溯，不得替代 V0.5 进行加工。
