# 从这里开始：Rack4Modules V0.6 稳定支撑版

V0.6 是当前设计入口。它保持 **3 排 3U、每排 104HP、没有 1U 行**，模块安装内净宽仍为
`542 mm`；本次只把折叠脚架收敛为一个可明确承载、可验证的正式工作姿态。

V0.5 已完成 `524 PASS / 15 WARNING / 0 FAIL` 的文件、引用和离散 CAD 几何校验，是 V0.6
双剪切夹持结构的已验证基线，但不等于连续运动或实物结构认证。V0.4 及更早版本继续保留为
历史，不应覆盖或删除。

## 1. 当前应打开哪个文件

先进入：

`C:\Users\LENOVO\Desktop\Rack4Modules\cad\assemblies`

正常查看机箱时，优先打开：

`Rack4Modules_OpenCase_V06.SLDASM`

检查正式桌面支撑姿态时，打开：

`Rack4Modules_DesktopTilt60_V06.SLDASM`

V0.6 目标总装如下；当前独立只读检查为 **579 PASS / 13 WARNING / 0 FAIL**，逐项证据以
[`reports/layout-v06-validation.md`](reports/layout-v06-validation.md) 为准。

| 文件名 | 用途 |
| --- | --- |
| `Rack4Modules_OpenCase_V06.SLDASM` | 正常开箱和主要设计入口 |
| `Rack4Modules_TransportClosed_V06.SLDASM` | 合盖运输包络检查 |
| `Rack4Modules_ClearanceCheck_V06.SLDASM` | 模块深度和内部避让检查 |
| `Rack4Modules_DesktopTilt60_V06.SLDASM` | 唯一正式桌面支撑姿态，模块面相对桌面约 60° |

运输态的盖子是独立零件 `DeepTravelLid_V06_StandRelief.SLDPRT`。它沿用 70 mm 线缆
净空，但在左右侧回边分别切出 `y=-155..41 mm、z=5..15 mm` 的折叠腿让位槽；这样折叠
后的 170 mm 脚架不会顶到盖子。V0.4 的 `DeepTravelLid_70mmClearance.SLDPRT` 没有被覆盖，
且运输装配的离散干涉 API 结果为 0。连续 Motion、制造公差和样机运输试验仍未完成。

V0.6 **不生成 75° 工作总装，也不把 75°列为功能**。V0.5 中的 75°文件只用于历史追溯，
不能据此宣称 V0.6 有第二个工作角度。倾角总装中的桌面是几何参照，不属于产品或 BOM。

`.SLDASM` 依赖 `cad/parts` 内的原生零件。不要只复制一个总装文件；交给另一台电脑或加工厂
时应保留整个 `Rack4Modules` 文件夹及其 Git 历史。

## 2. V0.6 为什么比 V0.5 更适合稳定支撑

V0.6 保留内外侧板夹住折腿的双剪切结构，同时把“承载”和“锁定”拆成独立载荷路径：

```text
桌面
  ↑ Ø16×6 橡胶圆冠脚垫
170 mm 折腿（6 mm 7075-T6）
  → 6×3×16 mm 钢制脚跟承压面
  → Ø8×12.8 mm 双剪切实体承载止挡
  → 3 mm 内框 + 每侧四个 6.8 mm 隔柱 + 3 mm 外颊板
```

`Ø8×12.8 mm` 双剪切主轴负责定位和转动；`Ø8×12.8 mm` 实体承载止挡承担展开后的主要
压紧反力。另设的 `Ø5×12.8 mm` 锁销只防止腿意外反折，并提供收纳锁定位，**不作为工作
载荷止挡**。这样，操作时的载荷不会主要压在一根小锁销或弹簧定位件上。

## 3. 当前冻结的关键尺寸

| 项目 | V0.6 名义值 |
| --- | ---: |
| Eurorack 格式 | `3 × 104HP`，无 1U |
| 模块安装内净宽 | `542 mm` |
| 内侧承力框 | `3 mm` |
| 腿腔/夹层 | `6.8 mm` |
| 折腿 | `6 mm 7075-T6` |
| 外承力颊板 | `3 mm` |
| 名义裸机总宽 | `567.6 mm` |
| 主轴 | `Ø8 × 12.8 mm`，双剪切 |
| 实体承载止挡 | `Ø8 × 12.8 mm`，双剪切 |
| 钢制脚跟承压面 | `6 × 3 × 16 mm` |
| 防反折/收纳锁销 | `Ø5 × 12.8 mm` |
| 内外侧板隔柱 | 每侧 4 个，长度 `6.8 mm` |
| 主轴至脚垫中心距 | `170 mm` |
| 脚垫 | `Ø16 × 6 mm` 氯丁橡胶（CAD 候选）圆冠，中心距桌面 `8 mm` |
| 正式工作姿态 | 模块面相对桌面约 `60°` |
| 预计折腿旋转角 | 约 `92.423°` |
| 预计后支撑深度 | 约 `133.770 mm` |

每侧隔柱的机箱坐标 `(y,z)` 为 `(-160,39)`、`(-160,68)`、`(-70,34)`、`(42,34)` mm。
原先位于前上方的隔柱会侵入折腿/脚垫扫掠，因此未保留；外颊板主片下缘降至 `z=24 mm`，
给两只 `z=34 mm` 隔柱外圆保留 `5 mm` 名义板边。V0.6 校验器会对这四点逐一做 2001 点二维扫掠，
要求与折腿、钢脚跟和脚垫的名义净距均不小于 `2 mm`。这仍不能替代含制造公差的三维运动验证。

`567.6 mm` 是当前名义裸机宽度，不是含深盖、盖锁、阳极层、公差和密封件的运输包络。
出图前仍须完成完整公差链。

## 4. 稳定性筛查应该怎样理解

当前几何的后支撑深度约为 `133.770 mm`，理想居中的重心投影距前后边界各约
`66.88 mm`。按 `20 kg` 满载、在 `300 mm` 高处施加 `20 N` 操作力、操作力取
`1.5` 安全系数的早期抗倾覆筛查，需要约 `45.87 mm` 的重心边界余量。

因此，装入最重目标模块后必须实测三维重心；当前目标是让纵向重心投影落在支撑深度中点
附近约 `±20 mm`，此时理论较小侧余量约为 `46.88 mm`。这个数字只表明目标几何能进入
下一轮样机试验，不能代替对真实脚垫接触、桌面摩擦、线缆拖拽和用户操作方向的测量。

按单腿 `400 N`、再取 `1.5` 倍载荷，并按接触法向到主轴的 `28.0 mm` 止挡力臂估算，
实体止挡处反力约为 `3.64 kN`。这只是确定止挡、钢脚跟和侧板局部设计量级的筛查工况，不是零件许用
载荷、有限元认证或整机额定载荷。

## 5. 仍必须完成的实物验证

V0.6 在以下项目全部完成前，不能宣称“已确保稳定”或直接投产：

1. 装入最重目标模块、电源、线缆和盖锁后，实测整机三维重心及最坏偏载重心；
2. 实测脚垫在目标桌面材料上的静摩擦和动态滑移，并验证橡胶脚垫机械卡持不会脱落；
3. 在最不利方向和位置施加 `20 N` 操作力，确认不倾覆、不滑移、不抬脚；
4. 做单腿 `400 N` 偏载并叠加 `30 N` 侧向载荷，检查永久变形、孔椭圆化和侧板张开；
5. 做 `1.5` 倍静载保持试验，并复核主轴、止挡、钢脚跟、隔柱和紧固件载荷路径；
6. 完成至少 `10000` 次折叠循环，检查磨损、间隙、锁销啮合和止挡面压痕；
7. 完成轴套、阳极层、板厚、平面度、隔柱和销轴的最坏公差链；
8. 冻结防松和轴向防脱方案，并完成运输振动及跌落后的复检。

## 6. Intellijel 参考边界

[Intellijel 7U Performance Case Gen-2 官方产品页](https://intellijel.com/shop/cases/7u/7u-performance-case-gen-2/)
及[官方手册](https://intellijel.com/downloads/manuals/7u/7u-performance-case-gen-2_manual_2026.04.13.pdf)
只能确认其采用“侧板内隐藏腿、独立弹簧 Stand Lock、两个位置”的功能组织。公开资料没有
证明其采用 V0.6 的单角、双剪切主轴、实体承载止挡、销轴尺寸、材料或载荷指标。

因此，V0.6 的单一 60°正式姿态、双剪切夹层、承载止挡与防反折锁销分工，都是
Rack4Modules 针对本机箱作出的原创工程选择，不应描述成对 Intellijel 内部结构的复制。

## 7. 验证与版本边界

- [项目总览](README.md)
- [V0.6 稳定支撑设计与验收边界](docs/stability-v06.md)
- [`reports/layout-v06-validation.md`](reports/layout-v06-validation.md)：V0.6 最终软件验证入口
- [V0.5 零基础说明](START_HERE_V05.md)：已验证双剪切基线与历史入口
- [V0.5 双剪切脚架说明](docs/layout-v05-captured-stand.md)
- [V0.4 历史入口](START_HERE_V04.md)

V0.6 当前自动验证统计为 **579 PASS / 13 WARNING / 0 FAIL**。WARNING 中仍保留
NeedsRegen 打开提示、故意的电源/模块保留空间包络交叠，以及必须在实物上完成的重心、摩擦、
载荷、疲劳、公差、供应商件和连续运动项目。零 FAIL 只代表报告覆盖的文件、引用和离散 CAD
条件满足，不代表连续运动、强度认证或实物稳定性试验通过。
