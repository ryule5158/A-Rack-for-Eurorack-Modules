# Rack4Modules V0.3 使用入口：从这里开始

这个项目交付的是一个 **3 排 104HP Eurorack 便携机箱的机械设计**。即使没有使用过
SolidWorks，也可以按照下面的步骤找到正确装配体、理解结构，并避开已经废弃的旧版本。

## 第一步：打开正确的机箱模型

1. 在 Windows 文件资源管理器中进入：

   `C:\Users\LENOVO\Desktop\Rack4Modules\cad\assemblies`

2. 找到并双击：

   **`Rack4Modules_OpenCase_V03.SLDASM`**

3. 如果双击没有自动启动 SolidWorks，请先打开 SolidWorks，再选择“文件 → 打开”，
   找到上面这个 `.SLDASM` 文件。
4. 打开后，鼠标滚轮可以缩放，按住鼠标中键可以旋转视角，按 `F` 可以让整个机箱重新
   适合屏幕。需要查看背面、上边和侧面时，可以使用右上角的视图立方体。

`.SLDASM` 是完整装配体；`.SLDPRT` 是单个零件。第一次查看时，应该打开上面的完整
装配体，而不是从零散零件开始。请保留整个 `Rack4Modules` 文件夹的目录结构；如果
只拷走装配体而遗漏 `cad\parts`，SolidWorks 可能找不到它引用的零件。

## 三个 V0.3 装配体分别有什么用

| 文件 | 使用场景 | 打开后看到的内容 |
| --- | --- | --- |
| `Rack4Modules_OpenCase_V03.SLDASM` | 首选入口、日常查看、结构讨论 | 打开的机箱、三排导轨、上后沿接口、侧面机构和背部 VESA 结构 |
| `Rack4Modules_TransportClosed_V03.SLDASM` | 检查盖子闭合、携带及运输外形 | 相同的 V0.3 机箱，加上可拆卸深盖 |
| `Rack4Modules_ClearanceCheck_V03.SLDASM` | 检查模块、电源和内部空间是否冲突 | 模块面板量规、模块深度包络以及两种尚未选定的电源预留空间 |

上述三个文件都位于 `cad\assemblies`。如果工厂没有 SolidWorks，可以使用
`exports` 文件夹里同名的 `_V03.STEP` 通用三维文件查看最终几何。

**注意：文件名没有 `_V03` 的装配体属于 V0.2 历史版本。** 例如：

- `Rack4Modules_OpenCase.SLDASM`
- `Rack4Modules_TransportClosed.SLDASM`
- `Rack4Modules_ClearanceCheck.SLDASM`

这些旧文件仅用于了解历史方案，**不能用于加工、下料、询价、采购或制造**。它们的
接口位置、提手、脚垫和其他细节与最终 V0.3 方案不同。

## 不需要学 SolidWorks，也能先看懂这几个关键点

### 1. 模块容量和机箱尺寸

- 正面共有 **3 排 3U，每排 104HP**，总计 312HP。
- 这是 **3 × 104HP** 方案，**没有 1U 排**。
- 不装深盖时，机身本体外形为 **548 × 420 × 110 mm**。
- 合上深盖后，运输状态外宽为 **552 mm**；盖子比 548 mm 机身略宽是为了包住箱体。
- 深盖在模块正面留出约 **70 mm** 空间，用来容纳旋钮和插着的连接线；是否能装下
  实际模块和接线，仍需要拿真实器件检查。

### 2. 接口不在背部大平面，而在上后沿

把模块所在的大面朝向自己，机箱上方那条狭长的后向边缘，就是接口和提手的位置。
从左到右依次为：

```text
8 个 6.35 mm TRS 音频接口预留  →  中央提手  →  3 个五针 MIDI + 1 个 USB-C  →  独立电源盲板
```

- 左侧是 **8 个 6.35 mm TRS** 接口的单排机械开孔。
- 中央是提手；最终握把宽 **126 mm**，安装孔距 **110 mm**。
- 右侧是 **3 个五针 DIN** 位置，可对应 MIDI IN / OUT / THRU，以及 **1 个 USB-C**
  的机械预留位置。
- 最右侧有一块独立的 **电源盲板**，目前没有电源插孔、开关或其他固定孔。

这些都只是接口安装位置，不表示机箱已经具有音频处理、MIDI 通信、USB 连接或供电功能。

### 3. 两侧、背面和下面分别是什么

- **左右短侧面**：各有一条内藏折叠支腿和两个内藏盖锁。腿和锁扣收纳后与机身侧面
  齐平，不应额外增加 548 mm 的机身宽度。支腿约 15° / 30° 的两档目前仍是设计目标。
- **背部整个大平面**：中央是 **VESA 100 × 100 mm、4 × M4** 支架安装位，四角
  附近有四个防滑脚垫；这里不布置音频、MIDI、USB、电源接口或通风孔。
- **下方狭长边缘**：设置隐藏式被动通风槽，不影响背部 VESA 安装面。

### 4. 模块安装深度不是所有位置都一样

| 机箱内部区域 | 当前设计允许的模块净深 | 应如何理解 |
| --- | ---: | --- |
| 没有电源或结构遮挡的常规区域 | 85 mm | 这是常规目标，并不代表整个机箱都能安装 85 mm 深的模块 |
| 分布式电源母线预留区域 | 73 mm | 如果在这里安装 85 mm 深的模块，会与母线预留空间重叠 12 mm |
| 集中式电源预留区域 | 60 mm | 如果在这里安装 85 mm 深的模块，会与电源预留空间重叠 25 mm |

在 `Rack4Modules_ClearanceCheck_V03.SLDASM` 中，这些模块与电源包络相互进入是为了
如实显示当前的空间限制，并不是已经安装好的真实电源或真实模块。电源型号确定后，
仍需要重新检查连接器尾部、线束、实际模块和内部加强件的净空。

## 加工前应该看哪些文件：以最终修正版为准

数字、尺寸或接口坐标出现矛盾时，按下面顺序判断：

1. **最终制造参数：`design\parameters-v0.3-fabrication.json`**。这里记录最终机身
   宽度、折腿和盖锁位置、126 mm 提手、接口中心坐标，以及 85 / 73 / 60 mm 分区净深。
2. **最终制造修正说明：`docs\manufacturing-v03-corrections.md`**。这里用中文解释
   为什么旧折腿、旧盖锁、132 mm 提手和“全区域 85 mm 净深”等说法不能继续使用。
3. **最终三维模型：`cad\assemblies` 中三个带 `_V03` 的 `.SLDASM`，以及 `exports`
   中对应的 `_V03.STEP`**。实际制造仍需结合分件工程图、零件尺寸和供应商图纸复核。
4. **其他 V0.3 说明**，例如 `docs\manufacturing-v03.md`、
   `docs\interface-layout-v03.md` 和 `design\parameters-v0.3.json`，只作为补充；
   与上面两份最终修正文件不一致时，必须以上面两份最终文件为准。

旧版 `design\parameters.json`、`docs\manufacturing.md`、
`docs\interface-layout.md` 和不带 `_V03` 的装配体只保留历史参考价值，不能直接作为
V0.3 制造依据。如果最终参数、最终修正说明和实际三维模型之间仍有冲突，应暂停出图
和加工，先确认工程变更。

## 设计参考的同类官方产品

本项目的“上后沿接口带 + 中央提手 + 侧面折腿/盖锁 + 大背面安装支架”布局，主要参考：

- [Intellijel 7U Performance Case Gen-2 官方产品页](https://intellijel.com/shop/cases/7u/7u-performance-case-gen-2/)
- [Intellijel 7U Performance Case Gen-2 官方用户手册](https://intellijel.com/downloads/manuals/7u-performance-case-gen-2_manual_2026.04.13.pdf)
- [Befaco 7U Case 官方产品页](https://www.befaco.org/befaco_7u_case/)
- [Befaco 7U Case 官方用户手册](https://www.befaco.org/docs/7U_CASE/7U_Case_User_Manual.pdf)

详细的对比、术语和布局依据见 `docs\layout-benchmark-v03.md`。参考同类产品的外形
和结构，不代表本项目已经具备这些产品内置的电源、音频、MIDI 或 USB 电路。

## 哪些事情目前还没有完成

当前成果是机械设计和空间预留，以下内容尚未设计完成或尚未通过实物验证：

- 电源型号、供电拓扑、输入接口、开关、电压、电流、保护电路、电源 PCB 和安装孔。
- 音频电路、MIDI 电路、USB 电路、接口转接板、实际连接器型号和内部线束。
- 支腿转轴、定位档位、盖锁锁舌、提手五金和供应商最终安装图。
- 接地、绝缘、EMC、电气安全、认证和完整热设计。
- 整机实际重量、VESA 承重、提手承重、支腿稳定、跌落、运输振动和散热实测。
- 最终钣金展开、折弯补偿、表面处理公差、正式制造图和量产放行。

因此，这个项目可以用于查看机械方案、继续完善结构、做供应商技术沟通和准备样机；
不能把尚未完成的供电、通信、承载或安全验证描述成已经实现或已经通过。
