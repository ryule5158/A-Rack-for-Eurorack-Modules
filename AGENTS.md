# Rack4Modules 协作与交接硬规则

本文件适用于 `C:\Users\LENOVO\Desktop\Rack4Modules` 及其全部子目录。进入本项目工作的任何代理都必须先读本文件和根目录 `WORKLOG.md`。

## 唯一工作空间

- 本工程唯一允许使用的工作空间是 `C:\Users\LENOVO\Desktop\Rack4Modules`。
- 禁止为了本工程在桌面重建或使用 `Euro_Rack`、`Euro_Rack*` 等平行目录；临时编译物也应放在本工程明确的临时/构建目录，并在记录后清理。
- 不得覆盖、移动、删除或暂存用户已有的无关修改。操作前后都要执行精确的 `git status --short --branch` 核对。
- 禁止使用 `git add .`。只能逐个精确暂存本次确认属于当前发布的文件。

## 工作记录是硬要求

- 每一项用户要求、每一次实际动作、涉及的文件、验证证据、失败或中断、回滚/废弃决定以及尚未完成项，都必须及时追加到根目录 `WORKLOG.md`。
- 开始动作前先写“计划/要求”，完成后紧接着写“结果/证据”。不得等到任务结束再凭记忆补写。
- 每条记录至少包含：北京时间、来源或用户原话摘要、动作、影响文件、客观证据、状态、下一步。
- 状态只允许使用：`用户要求`、`计划`、`进行中`、`已验证`、`历史宣称-待复核`、`失败/中断`、`回滚/废弃`、`待办`。
- CAD/脚本/报告存在不等于通过；必须写清验证边界。质量属性不是实物称重，零干涉不是强度/疲劳/跌落认证。

## 当前冻结要求

- 机箱为 3 排 104HP、全 3U、无 1U，总容量 312HP/9U，带可牢固锁定且可拆下展示的深盖。
- 便携、结实、可靠优先于极限减重；任何减重若可能削弱承力路径，必须取消。
- 只有顶部/上后沿一个承力提手，不设侧把手。
- 折叠脚架必须夹在壳体/侧框结构中，能稳定形成锐角工作姿态；外侧脚架夹板保持全区域 4 mm，不允许轻量化。
- 模块导轨采用用户最新确认的自制方案：以 V07 粗壮闭口截面为基线，定型为全长 `542 mm` 的恒定截面 6061-T6 挤压件、可更换 AISI304 M3 螺纹条及独立 7075 端部 M4 插芯。中央可用安装长度必须为 `528.32 mm / 104HP`、M3 孔距 `5.08 mm`；每个导轨端必须保留独立 M3 定位与 M4 结构紧固，禁止恢复成单薄条或仅靠单颗螺钉/摩擦夹持。Tiptop Z-Rails 仅作为商业基准，不再是最终采购冻结件。
- 接口位于上侧或左右侧面，不布置在背面大平面；MIDI 5 针与 USB 可排成一排，音频接口可原创布局，并给外置电源适配器入口/面板留足空间。
- 电源尚未冻结；当前机械任务只留可维护空间。供电规划基线是认证外置低压适配器加箱内分排 DC/DC，而不是把未经认证的市电反激直接放入乐器箱。
- 背面保留 VESA 100 安装能力；最终承力、拉脱和公差必须由结构计算、样机和供应商件确认。
- 外观遵循 Rymovia 品牌资料；用户已选定 A 方案的环形/相位回廊图案，盖子与背板均需有协调图案。

## 验证与发布边界

- 发布前至少重验 OpenCase、TransportClosed、ClearanceCheck、Tilt60、Showcase 五种状态；非量规总装应完成原生装配与 STEP 回读、组件/引用/变换检查和严格干涉检查。
- Tilt60 必须单独记录重心、支撑多边形和防倾覆边界；不能只凭外观判断稳定。
- 实物前仍必须保留：满载/偏载、单腿侧载、提手提拉、VESA 拉脱、紧固件防松、10000 次折叠/锁止、振动、跌落、摩擦污染、公差链、真实模块首件装配、供应商 COTS 配合等物理闸门。
- 在五态 CAD 验证完成、废弃原型清理、文档/BOM/制造边界齐全之前，不提交最终发布，不推送远端。
- 最终向用户展示时关闭无关窗口，只保留最终 SolidWorks 成品视图；盖子放在箱体旁边，不遮挡内部。

## 安全与授权

- 不执行破解许可服务器、盗版激活器或可疑 FlexNet 脚本。
- 删除必须遵守用户给出的精确路径范围；不得扩大到相邻产品、版本或目录。
- SolidWorks 正在占用的文件先关闭对应文档/进程并复核路径，不能强删或波及用户文件。
